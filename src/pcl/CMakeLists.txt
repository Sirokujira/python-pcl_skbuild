# Cython extension modules for the pcl package.
# The pyx files cimport declarations from src/pcl/pxd/ (auto-generated by
# cppast2autopxd; regenerate with `python pxdgen/generate.py`).
#
# To add a module: write src/pcl/<name>.pyx and append <name> to the list.
# Every module gets the same treatment, so there is nothing else to keep in
# sync — the per-module boilerplate the template spells out by hand is a
# loop here because the module count grows with the wrapped PCL surface.
set(PCL_CYTHON_MODULES
    _pointcloud
    _filters
    _kdtree
    _segmentation
    _grabber
    _octree
    _features
    _surface
    _registration
    _pointtypes
    _keypoints
)

foreach(module ${PCL_CYTHON_MODULES})
    add_cython_target(${module} CXX PY3)
    add_library(${module} MODULE ${${module}})
    target_link_libraries(${module} ${PCL_LIBRARIES})
    python_extension_module(${module})
    install(TARGETS ${module} LIBRARY DESTINATION src/pcl)
endforeach()
