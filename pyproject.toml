[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "scikit-build>=0.17",
    "cmake>=3.18",
    "ninja; platform_system!='Windows'",
    "cython>=3.0",
    "numpy",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
